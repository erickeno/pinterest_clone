<h2><%= @board.title %></h2>
<p><%= @board.description %></p>

<%= link_to "Edit Board", edit_board_path(@board) %>

<%= link_to "Delete Board", board_path(@board), method: :delete, data: { confirm: "Are you sure you want to delete this project?" } %>
